fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.31
gradio==6.7.0
requests==2.33.0
Pillow==12.2.0
transformers==5.0.0rc3
mcp==1.23.0

# --- Vision model dependencies ---
# Install torch SEPARATELY per backend/README.md instructions, since the
# correct wheel depends on whether you're running on an AMD (ROCm), NVIDIA
# (CUDA), or CPU-only machine. Do not blindly `pip install torch` here.

# --- Optional: real Google ADK integration (backend/agents/adk_integration.py) ---
# google-adk
